## AI 自动回复系统操作手册

本系统支持本地 Ollama 大模型、远程 API、企业微信自建应用和企业微信智能机器人四种接入模式。通过 wxautoz UI 自动化直连微信 PC 客户端，内置博查搜索和智能搜索触发机制，可回答实时性问题。

---

### 系统架构

```
个人微信 (auto-reply.bat)
    ↕ wxautoz UI 自动化
auto_reply.py (Python)
    ├── 本地模式 → Ollama (端口 11434, qwen3:8b)
    ├── 远程模式 → apiyihe 代理 → gemini-3.5-flash / glm-5.1 / qwen3.7-max
    ├── Qoder CLI 模式 → qoderclicn -p (自带搜索, 默认 Qwen3.7-Max)
    └── Qoder 模式 → Qoder 中继 (端口 11435) → qoder_bridge.py → Ollama

企业微信自建应用 (wecom/wecom.bat)
    ↕ HTTP 回调 (Flask, 端口 8080)
wecom/server.py → shared/ai_engine.py → 博查搜索 + 远程 API

企业微信智能机器人 (wecom_bot/bot.bat)
    ↕ WebSocket 长连接 (wecom_aibot_sdk)
wecom_bot/bot.py → shared/ai_engine.py → 博查搜索 + 远程 API

微信客服 (customer_service/customer_service.bat)
    ↕ HTTP 回调 (Flask, 端口 8081)
customer_service/server.py → shared/ai_engine.py → kf/send_msg 回复客户
```

辅助服务（可选）：

| 组件 | 版本 | 端口 | 说明 |
|------|------|------|------|
| Ollama | v0.30.6 | 11434 | 本地大模型推理引擎，模型存储在 D:\AI\models |
| Open WebUI | v0.9.6 | 3000 | Web 聊天界面 |
| MCPo | v0.0.20 | 8000 | MCP 协议转 OpenAPI（可选） |
| Qoder 中继 | - | 11435 | auto_reply.py 内置 HTTP 中继 |
| WeCom 回调 | - | 8080 | 企业微信自建应用回调服务器 |
| 微信客服 | - | 8081 | 微信客服回调服务器 |

---

### 目录结构

```
D:\AI\
├── code/                        # 代码仓库 (git 管理)
│   ├── auto-reply.bat           # 启动微信自动回复（模型选择菜单）
│   ├── start.bat                # AI Station 一键启动
│   ├── stop.bat                 # 一键停止所有服务
│   ├── auto_reply.py            # 自动回复主程序（独立搜索实现）
│   ├── qoder_bridge.py          # Qoder 中继桥接脚本
│   ├── qoder_long_poll.py       # Qoder 长轮询脚本
│   ├── shared/                  # 共享模块
│   │   ├── ai_engine.py         # AI 回复引擎（远程 + Ollama + 搜索）
│   │   └── search_engine.py     # 搜索引擎（博查/Bing/百度 + 天气）
│   ├── wecom/                   # 企业微信自建应用
│   │   ├── server.py            # Flask 回调服务器
│   │   ├── handler.py           # 消息处理器
│   │   └── wecom.bat            # 启动脚本
│   ├── wecom_bot/               # 企业微信智能机器人
│   │   ├── bot.py               # WebSocket 客户端
│   │   └── bot.bat              # 启动脚本
│   ├── customer_service/        # 微信客服模块
│   │   ├── server.py            # Flask 回调服务器 (端口 8081)
│   │   ├── handler.py           # 消息处理器 (CSHandler)
│   │   ├── diagnose.py          # API 连通性诊断工具
│   │   ├── create_group.py      # appchat 群创建工具
│   │   ├── customer_service.bat # 启动脚本
│   │   └── config.json.example  # 配置模板
│   ├── 操作手册.md              # 本文件
│   └── 技术实现文档.md          # 技术实现细节
│
└── workspace/                   # 运行时数据 (不纳入 git)
    ├── model_config.json        # 模型配置（API key、bocha_api_key）
    ├── auto_reply_config.json   # 自动回复配置
    ├── wecom_config.json        # 企业微信自建应用凭证
    ├── wecom_bot_config.json    # 企业微信智能机器人凭证
    ├── customer_service_config.json  # 微信客服配置（含转发设置）
    ├── models/                  # Ollama 模型 (D:\AI\models)
    ├── auto_reply.log           # 运行日志
    └── open-webui-data/         # Open WebUI 数据
```

---

### 快速开始

#### 模式一：个人微信自动回复（auto-reply.bat）

确保微信 PC 版 3.9.x 已启动并登录，然后双击 `auto-reply.bat`。

```bash
# 命令行参数（跳过交互菜单）
python auto_reply.py --remote qwen3.7-max   # 指定远程模型
python auto_reply.py --remote               # 使用上次远程模型
python auto_reply.py --last                 # 使用上次配置
```

机器人行为：

- 私聊：自动回复所有消息
- 群聊：仅 @提及时回复
- 天气查询：博查网络搜索优先，wttr.in API 补充
- 实时性问题：模型回避时自动博查搜索后重试

#### 模式二：AI Station（start.bat）

双击 `start.bat` 启动完整 AI 工作站。远程模式跳过 Ollama，本地模式依次启动 Ollama + MCPo + Open WebUI。

远程模式从 `workspace/model_config.json` 读取 `api_base` 和 `api_key`，启动脚本中不保存密钥。

#### 模式三：企业微信自建应用（wecom/wecom.bat）

需要公网 IP 或内网穿透。详见 [wecom/README.md](wecom/README.md)。

1. 在企业微信管理后台创建自建应用，获取 CorpID、Secret、Token、EncodingAESKey
2. 配置 `workspace/wecom_config.json`
3. 配置公网回调地址（如 `http://43.143.110.230:8080/callback`）
4. 双击 `wecom/wecom.bat` 启动

#### 模式四：企业微信智能机器人（wecom_bot/bot.bat）

无需公网 IP，通过 WebSocket 长连接企业微信服务器。

1. 在企业微信管理后台创建智能机器人，获取 BotID 和 Secret
2. 配置 `workspace/wecom_bot_config.json`：
   ```json
   {"bot_id": "YOUR_BOT_ID", "secret": "YOUR_BOT_SECRET"}
   ```
3. 双击 `wecom_bot/bot.bat` 启动

#### 模式五：微信客服（customer_service/customer_service.bat）

让个人微信用户通过微信客服窗口与 AI 对话，无需企业微信账号。

1. 在企业微信管理后台启用"微信客服"，创建客服账号
2. 在自建应用的"可调用接口的应用"中授权客服账号
3. 将服务器 IP 加入自建应用的企业可信 IP 白名单
4. 配置 `workspace/customer_service_config.json`：
   ```json
   {
     "corp_id": "YOUR_CORP_ID",
     "corp_secret": "YOUR_CORP_SECRET",
     "token": "YOUR_CALLBACK_TOKEN",
     "encoding_aes_key": "YOUR_43_CHAR_AES_KEY",
     "port": 8081,
     "forward_userid": "ZhouLei",
     "agent_id": "1000002"
   }
   ```
5. 配置公网回调地址（如 `http://43.143.110.230:8081/callback`）
6. 双击 `customer_service/customer_service.bat` 启动
7. 诊断工具：`python customer_service/diagnose.py --config workspace/customer_service_config.json`

---

### 配置文件说明

#### model_config.json（模型配置）

```json
{
  "mode": "remote",
  "model": "qwen3.7-max",
  "api_base": "https://z.apiyihe.org/v1",
  "api_key": "sk-xxx",
  "remote_provider": "Gemini Flash (apiyihe)",
  "bocha_api_key": "sk-xxx"
}
```

| 字段 | 说明 |
|------|------|
| `mode` | `ollama`（本地）/ `remote`（远程 API）/ `qodercli`（Qoder CLI）/ `qoder`（Qoder 中继） |
| `model` | 模型名称 |
| `api_base` | 远程 API URL（仅 remote 模式） |
| `api_key` | 远程 API 密钥（仅 remote 模式） |
| `remote_provider` | 提供商显示名称 |
| `bocha_api_key` | 博查搜索 API 密钥（[申请](https://open.bochaai.com)） |
| `relay_port` | Qoder 中继端口（仅 qoder 模式，默认 11435） |
| `qodercli_model` | Qoder CLI 模型名（仅 qodercli 模式，默认 `Qwen3.7-Max`） |
| `qodercli_max_tokens` | 最大输出 token（仅 qodercli 模式，默认 4096） |
| `qodercli_permission_mode` | `bypass`（自动批准所有权限请求）/ `ask`（将权限请求发给用户确认后执行，默认 bypass） |

注意：`bocha_api_key` 是搜索功能的核心后端，未配置时搜索回退到 DuckDuckGo/Bing。程序采用合并保存模式，切换模型时不会丢失此字段。

qodercli 模式使用 `qodercli_permission_mode` 控制权限请求行为：`bypass` 模式下 qoderclicn 自动批准所有操作；`ask` 模式下先以"仅分析不执行"方式获取计划，发送给用户确认后再恢复会话执行。

#### wecom_config.json（企业微信自建应用）

```json
{
  "corp_id": "ww202e2b0f068207c8",
  "corp_secret": "8hnirjaVzUDe...",
  "agent_id": "1000002",
  "token": "HCVRIfDtR4kDsPq83Bu",
  "encoding_aes_key": "BfJeIkRFtAHGk...",
  "host": "0.0.0.0",
  "port": 8080
}
```

#### wecom_bot_config.json（企业微信智能机器人）

```json
{
  "bot_id": "aibO4tuQnrkf1HgOGcGbxzph1F45HokRIEI",
  "secret": "zQ8cG8u7As4OyeE0..."
}
```

#### customer_service_config.json（微信客服）

```json
{
  "corp_id": "ww202e2b0f068207c8",
  "corp_secret": "8hnirjaVzUDe...",
  "token": "HCVRIfDtR4kDsPq83Bu",
  "encoding_aes_key": "BfJeIkRFtAHGk...",
  "host": "0.0.0.0",
  "port": 8081,
  "open_kfid": "客服账号ID（可留空自动获取）",
  "forward_chatid": "转发群chatid（可选）",
  "forward_userid": "转发用户userid（可选，优先级高于chatid）",
  "agent_id": "自建应用agentid（forward_userid时必填）"
}
```

#### auto_reply_config.json（自动回复行为）

修改后无需重启，脚本每个轮询周期自动热加载。

**通用设置**

| 字段 | 说明 |
|------|------|
| `enabled` | 总开关，false 时停止所有回复 |
| `reply_delay` | 回复前等待秒数，默认 2 |
| `log_file` | 日志文件名，默认 `auto_reply.log` |

**私聊设置**

| 字段 | 说明 |
|------|------|
| `dm_enabled` | 私聊回复开关 |
| `dm_system_prompt` | 私聊人设提示词 |
| `dm_blacklist` | 黑名单 |
| `dm_whitelist_mode` | 白名单模式开关 |
| `dm_whitelist` | 白名单 |

**群聊设置**

| 字段 | 说明 |
|------|------|
| `group_enabled` | 群聊回复开关 |
| `group_system_prompt` | 群聊人设提示词 |
| `group_blacklist` | 群黑名单 |
| `group_trigger_words` | 触发词（包含时即使无 @ 也回复） |
| `group_mention_names` | @名字列表，如 `["@机器人"]` |

---

### 搜索系统

#### 搜索触发方式

系统通过四条路径自动触发搜索，无需手动干预：

1. **模型主动请求**：模型输出 `[SEARCH: 关键词]`，系统自动搜索
2. **回避自动检测**：模型回复含"查不到""连不上网"等回避用语，自动搜索重试（最多 2 轮）
3. **关键词匹配**：用户消息含"天气""新闻""今天"等词且回复过短时触发
4. **天气专属路径**：天气查询优先博查搜索，wttr.in 作为补充

#### 搜索后端瀑布

博查 API（国内最稳定） → DuckDuckGo/Bing → cn.bing.com 抓取 → 百度抓取

#### 回避检测机制

- 短语匹配：覆盖正式表达和口语化变体（"查不到""连不上网""没法帮你"等）
- 正则模式：模糊匹配间距（如"我...没法...帮"）
- 长回复容忍：回复超过 100 字时，需 2 个以上回避短语命中才判定

#### 日志追踪

搜索链路日志前缀：

| 前缀 | 含义 |
|------|------|
| `[搜索链路]` | 搜索流程主日志 |
| `[搜索判定]` | 搜索意图和回避检测结果 |
| `[搜索]` | 搜索引擎执行日志（Bocha/DDG/Bing） |
| `[天气API]` | wttr.in 天气 API 日志 |

---

### 远程部署注意事项

代码在 `code/` 目录（git 管理），运行时数据在 `workspace/` 目录（不在 git 中）。

远程服务器部署流程：

```bash
# 1. 拉取最新代码
cd /d C:\AI\AICChatDemo
git pull --rebase

# 2. 同步代码到运行目录（如果运行目录与 git 仓库不同）
xcopy C:\AI\AICChatDemo\auto_reply.py D:\AI\code\auto_reply.py /Y

# 3. 重启进程（Python 只在启动时加载代码，git pull 后必须重启）
# 关闭当前 auto-reply 窗口，重新双击 auto-reply.bat
```

重要：`workspace/model_config.json` 不在 git 中，首次部署或密钥变更时需手动同步。

---

### 常见问题

**Q: bat 文件闪退或中文乱码**
所有 .bat 文件必须使用 GBK 编码（cmd.exe 代码页 936）。

**Q: 自动回复连不上微信**
确认微信 PC 版 3.9.x 已启动并登录。wxautoz 需要微信窗口可见（可最小化但不能关闭）。

**Q: 搜索没走博查，日志显示 `[搜索] Bocha 跳过: 未配置 bocha_api_key`**
检查 `workspace/model_config.json` 是否包含 `bocha_api_key` 字段。该文件不在 git 中，需手动配置。

**Q: 搜索回退没触发**
查看 `[搜索链路]` 和 `[搜索判定]` 日志行。如模型用了新的回避表达，需在 `EVASIVE_PHRASES` 列表中补充。

**Q: 远程 API 回复被截断**
reasoning 模型消耗大量 token 用于思考，已设置 max_tokens=4096 并添加 finish_reason=length 警告。

**Q: 天气查询不准或超时**
博查搜索已作为天气查询首选后端（国内稳定），wttr.in 作为补充。如果博查未配置，天气查询会超时。

**Q: git pull 后代码没生效**
Python 只在启动时加载代码文件，`git pull` 后必须重启进程（关闭当前窗口，重新启动 bat）。

**Q: 企业微信消息发送失败 send_text 报错**
wechatpy.enterprise 的 `send_text()` 使用位置参数 `(agent_id, user_ids, content)`，不支持关键字参数。

**Q: Ollama 模型加载失败**
qwen3:8b 约 5.2GB，首次加载到 8GB 显存需几秒。关闭其他占用 GPU 的程序。

---

### 手动命令参考

```bash
# Ollama
ollama list                      # 查看已安装模型
ollama pull qwen3:8b             # 下载模型
ollama serve                     # 手动启动服务

# 自动回复
python auto_reply.py --remote qwen3.7-max   # 远程模式
python auto_reply.py --last                 # 使用上次配置

# Qoder 桥接
python qoder_bridge.py --port 11435

# 企业微信
# wecom/wecom.bat                # 自建应用回调服务器
# wecom_bot/bot.bat              # 智能机器人 WebSocket

# 微信客服
# customer_service/customer_service.bat  # 客服回调服务器
python customer_service/diagnose.py --config workspace/customer_service_config.json  # API诊断
python customer_service/create_group.py --config workspace/customer_service_config.json --list-users  # 查看用户

# 辅助服务
mcpo --config D:\AI\mcp\config.json --port 8000
open-webui serve --host 127.0.0.1 --port 3000
```
