## 本地 AI + 微信自动回复技术实现文档

本文档记录系统各组件的技术实现细节，供开发调试和二次扩展参考。

---

### 整体技术栈

```
接入层（五套独立实现，共享 AI 引擎）:

① 个人微信 — auto_reply.py (wxautoz UI 自动化)
② 企业微信自建应用 — wecom/ (Flask HTTP 回调)
③ 企业微信智能机器人 — wecom_bot/ (WebSocket 长连接)
④ Open WebUI — 浏览器聊天界面 (端口 3000)
⑤ 微信客服 — customer_service/ (Flask HTTP 回调, 端口 8081)

AI 引擎层:
    ├── 本地 Ollama (端口 11434, /api/chat, qwen3:8b)
    ├── 远程 API (apiyihe 代理, OpenAI 兼容 /v1/chat/completions)
    │       ├── 搜索瀑布: Bocha API → ddgs(Bing) → cn.bing 抓取 → Baidu 抓取
    │       ├── 天气查询: Bocha 搜索 → wttr.in (免费天气 API)
    │       └── 智能搜索触发: [SEARCH:] 标记 / 回避检测 / 关键词匹配 / 天气路径
    ├── Qoder CLI (qoderclicn -p 子进程调用, 自带搜索能力, 不走博查搜索)
    └── Qoder 中继 (端口 11435, ThreadingHTTPServer)
            → qoder_bridge.py → Ollama
            → qoder_long_poll.py → QoderWork cron 任务

辅助服务:
    Open WebUI (Python/FastAPI, 端口 3000) → Ollama / 远程 API
    MCPo (Python/FastAPI, 端口 8000) → weixin-mcp (可选)
```

**重要架构说明**：auto_reply.py 拥有独立的搜索实现（EVASIVE_PHRASES、web_search、weather_search 等函数全部内置），不依赖 `shared/search_engine.py`。`shared/` 模块仅被 wecom/ 和 wecom_bot/ 使用。修改搜索逻辑时必须同时更新 auto_reply.py 和 shared/search_engine.py 两处。

---

### 1. 微信接入层

系统有四套独立的微信接入实现，共享相同的 AI 引擎和搜索能力。

#### 1.1 个人微信 — wxautoz UI 自动化

wxautoz 是基于 Windows UI Automation 的微信自动化库，直连微信 PC 客户端，不依赖任何中间服务。

| 项目 | 值 |
|------|------|
| 安装方式 | `pip install wxautoz` |
| 要求 | 微信 PC 版 3.9.x 已启动并登录 |
| 连接方式 | Windows UI Automation API |
| 消息读取 | `GetNextNewMessage()` |
| 消息发送 | `ChatWith(who)` + `SendMsg(text)` |

`GetNextNewMessage()` 返回结构：

```python
{
    'chat_name': str,      # 聊天名称（联系人昵称或群名）
    'chat_type': str,      # 'friend'（私聊）或 'group'（群聊）
    'msg': [Message, ...]  # 消息列表
}
```

旧方案使用 weixin-mcp + MCPo + ClawBot API，依赖微信官方 ClawBot 服务（仅支持 1 对 1 私聊，不支持群聊）。wxautoz 通过 UI 自动化直接操控微信客户端，支持私聊和群聊，且无需额外的 MCP 中间层。

注意事项：wxautoz 依赖活跃的 GUI 会话，RDP 断开后会话锁定导致卡死。解决方法：使用 `tscon <会话ID> /dest:console` 将会话转移到控制台保持桌面活跃。

#### 1.2 企业微信自建应用 — wecom/ (HTTP 回调)

经典的 Webhook 架构，企业微信云端将加密 XML 消息推送到自建服务器。

| 项目 | 值 |
|------|------|
| 入口文件 | `wecom/server.py` (Flask) |
| 监听地址 | 0.0.0.0:8080 |
| 认证凭据 | corp_id + corp_secret + token + encoding_aes_key |
| 消息协议 | 加密 XML (wechatpy.enterprise.crypto.WeChatCrypto) |
| 回复方式 | WeChatClient.message.send_text() API 调用 |
| 公网要求 | 需要公网 IP 或内网穿透 |

流程：

```
企业微信云端 → POST /callback (加密 XML)
    → server.py 解密 + 解析消息
    → 启动后台线程处理 (满足 <5s 响应要求)
    → handler.py: 调用 shared/ai_engine.generate_reply()
    → WeChatClient.message.send_text(agent_id, user_id, reply)
```

handler.py 维护每个会话的对话历史（key: `user:{user_id}` 私聊 / `group:{chat_id}` 群聊），支持 2 秒速率限制和配置热加载。群聊通过 `chat_id` 区分群/私聊，新增 `group_system_prompt` 和 `group_enabled` 开关。

凭据配置文件：`workspace/wecom_config.json`（不进 git）。

#### 1.3 企业微信智能机器人 — wecom_bot/ (WebSocket)

更简单的接入方式，无需公网 IP。

| 项目 | 值 |
|------|------|
| 入口文件 | `wecom_bot/bot.py` (asyncio) |
| 连接方式 | WebSocket 长连接 (wss://openws.work.weixin.qq.com) |
| 认证凭据 | bot_id + secret |
| 消息协议 | JSON 帧 (wecom_aibot_sdk.WSClient) |
| 回复方式 | reply_stream(frame, stream_id, content, finish=True) |
| 公网要求 | 无需公网 IP |

流程：

```
bot.py → WSClient(bot_id, secret) → WebSocket 连接
    → 注册事件: message.text / event.enter_chat
    → on_text: asyncio.run_in_executor → shared/ai_engine.generate_reply()
    → client.reply_stream(frame, stream_id, reply, finish=True)
```

关键细节：
- 文本回复必须用 `reply_stream()`，不能用 `reply()` + msgtype:"text"（会报 40008 错误）
- `frame` 是 dict，消息内容在 `frame["body"]["text"]["content"]`
- 支持用户进入聊天窗口时发送欢迎消息（`event.enter_chat`）
- AI 调用通过 `run_in_executor` 避免阻塞 WebSocket 事件循环

凭据配置文件：`workspace/wecom_bot_config.json`（不进 git）。

#### 1.4 微信客服 — customer_service/ (HTTP 回调)

让个人微信用户通过微信客服窗口与 AI 对话，无需企业微信账号。

| 项目 | 值 |
|------|------|
| 入口文件 | `customer_service/server.py` (Flask) |
| 监听地址 | 0.0.0.0:8081 |
| 认证凭据 | corp_id + corp_secret + token + encoding_aes_key |
| 消息协议 | 加密 XML 通知 → kf/sync_msg 拉取实际消息 |
| 回复方式 | kf/send_msg API |
| 转发方式 | message/send（个人）或 appchat/send（群） |
| 公网要求 | 需要公网 IP 或内网穿透 |
| IP 白名单 | 服务器 IP 须加入自建应用的企业可信IP（否则 errcode 60020） |

流程：

```
个人微信用户 → 扫码进入客服窗口 → 发送消息
    → WeCom 云端 POST /callback (kf_msg_or_event 通知)
    → server.py 解密通知, 返回 "success" (< 5s)
    → 后台线程: handler.py sync_and_process()
        → kf/sync_msg 拉取实际消息 (需 open_kfid)
        → _dispatch() 过滤: 仅处理 origin=3 (客户消息), msgid 去重
        → _handle_text(): 调用 shared/ai_engine.generate_reply()
        → kf/send_msg 回复客户
        → (可选) message/send 或 appchat/send 转发到内部
```

关键细节：
- `kf/sync_msg` 必须传 `open_kfid` 参数（否则 errcode 95000）
- 自建应用须在企微后台"可调用接口的应用"中授权客服账号（否则 errcode 48007）
- 多条回调通知可能对应同一消息，通过 `msgid` 去重
- 启动时先 drain 积压消息（推进 cursor 不处理），只处理启动后的新消息
- 配额：每客户每 48 小时窗口 5 条服务端消息，用户每次发消息重置窗口

配置文件：`workspace/customer_service_config.json`（不进 git）。

辅助工具：
- `diagnose.py` — 逐步测试 API 连通性（token → account/list → sync_msg → send_msg）
- `create_group.py` — 创建 appchat 群并列出企业用户

---

### 2. 多模型后端

auto_reply.py 和 shared/ai_engine.py 均支持三种模型后端，通过 `model_config.json` 切换。

#### 2.1 本地 Ollama

Ollama 是用 Go 编写的本地 LLM 推理服务，提供兼容 OpenAI 的 REST API。

| 项目 | 值 |
|------|------|
| 版本 | v0.30.6 |
| 监听地址 | http://127.0.0.1:11434 |
| 模型存储 | D:\AI\models (通过 OLLAMA_MODELS 环境变量) |
| GPU | NVIDIA RTX 4070 Laptop, 8GB VRAM |
| 模型 | qwen3:8b (Q4_K_M, ~4.87GB) |

调用方式 — `generate_reply_ollama()`：

```python
POST http://127.0.0.1:11434/api/chat
{
  "model": "qwen3:8b",
  "messages": [...],
  "stream": false,
  "options": {"temperature": 0.8, "num_predict": 512}
}
```

qwen3 thinking 模式处理：如果 `message.content` 为空，从 `message.thinking` 中提取最后一段作为回复。

#### 2.2 远程 API (apiyihe 代理)

通过 OpenAI 兼容的 Chat Completions API 调用远程大模型。

| 项目 | 值 |
|------|------|
| API 端点 | https://z.apiyihe.org/v1/chat/completions |
| 支持模型 | gemini-3.5-flash, glm-5.1, qwen3.7-max |
| max_tokens | 4096（reasoning 模型需要足够 token，否则思考过程占用大部分额度导致内容截断） |
| temperature | 0.8 |

调用方式 — `generate_reply_remote()`：

```python
POST {api_base}/chat/completions
Headers: Authorization: Bearer {api_key}
{
  "model": "gemini-3.5-flash",
  "messages": [...],
  "temperature": 0.8,
  "max_tokens": 4096
}
```

注意事项：

- apiyihe 代理仅支持 `/v1/chat/completions`，不支持 `/v1/responses`（500 not implemented）
- reasoning 模型（如 gemini-3.5-flash）的 reasoning_tokens 计入 max_tokens，512 tokens 中约 491 用于思考，导致内容截断。必须设置 max_tokens >= 4096
- 检查 `finish_reason: "length"` 可判断回复是否被截断
- system prompt 追加禁止 tool call 输出的指令，防止模型输出 JSON 工具调用格式

#### 2.3 Qoder CLI (qoderclicn)

通过子进程调用本地 `qoderclicn` CLI 工具生成回复，qoderclicn 自带搜索能力，不走博查搜索引擎。

| 项目 | 值 |
|------|------|
| 二进制路径 | `~/.qoder-cn/bin/qoderclicn/qoderclicn.exe`（自动检测） |
| 默认模型 | Qwen3.7-Max |
| 调用模式 | `-p`（print 模式，非交互式输出到 stdout） |
| 权限模式 | `bypass`（自动批准）或 `ask`（向用户确认后执行） |
| 超时 | 180 秒 |
| 搜索引擎 | 不使用博查（qoderclicn 内置搜索工具） |

调用方式 — bypass 模式（`generate_reply_qodercli()`）：

```python
subprocess.run(
    [cli_path, "-p",
     "--system-prompt", system_prompt,
     "--max-output-tokens", "4096",
     "-m", "Qwen3.7-Max",
     "--permission-mode", "bypass_permissions",
     full_prompt],
    capture_output=True, text=True, timeout=180,
    env=clean_env,  # 清除 SDK 环境变量
)
```

调用方式 — ask 模式（两阶段权限确认）：

当 `qodercli_permission_mode` 设为 `"ask"` 时，handler 走两阶段流程：

```
Phase 1: generate_reply_qodercli_plan()
    → --permission-mode dont_ask + --session-id <uuid>
    → prompt 包装为"先分析步骤，不执行"
    → 检测输出是否含计划指标（步骤/需要执行/plan to 等）
    → 如果是计划: 返回 {"type": "permission_needed", "plan": ..., "session_id": ...}
    → 如果是直接回复: 返回 {"type": "reply", "content": ...}

用户确认: handler 发送计划给用户，等待 确认/取消

Phase 2: generate_reply_qodercli_execute()
    → --permission-mode bypass_permissions + --resume <session_id>
    → 用户批准: 恢复会话自动执行
    → 用户取消: 发送取消消息，不恢复
```

handler.py 中 `_handle_text()` 三路分发：

```python
# 1. 有待确认的 session → 处理用户回复（确认/取消）
if conv_key in self.pending_sessions:
    return self._handle_permission_response(...)

# 2. ask 模式 → 两阶段权限确认
if mode == "qodercli" and self.qodercli_permission_mode == "ask":
    return self._handle_qodercli_ask(...)

# 3. 常规模式 → 直接生成回复
reply = generate_reply(...)
```

注意事项：
- 必须清除 SDK 环境变量（`QODER_AGENT_SDK_ENTRYPOINT`、`QODER_SDK_AUTH_PAYLOAD_FILE`、`QODER_WORK_INTEGRATION_MODE`、`QODER_SCENE`），否则 qoderclicn 会进入 SDK 模式报错
- 对话历史格式化为文本嵌入 prompt（每次调用是无状态的）
- 需要先执行 `qoderclicn login` 完成登录

配置字段：

| 字段 | 说明 |
|------|------|
| `mode` | 设为 `"qodercli"` |
| `qodercli_path` | 二进制路径（可选，默认自动检测） |
| `qodercli_model` | 模型名（默认 `Qwen3.7-Max`） |
| `qodercli_max_tokens` | 最大输出 token（默认 4096） |
| `qodercli_permission_mode` | `"bypass"`（自动批准所有权限请求）或 `"ask"`（将权限请求转发给用户确认后执行，默认 bypass） |

#### 2.4 Qoder 中继

auto_reply.py 内置一个 ThreadingHTTPServer 中继服务器（端口 11435），将消息转发给 Qoder 桥接脚本或 QoderWork cron 任务处理。

中继服务器使用 ThreadingHTTPServer（而非 HTTPServer），因为 `/qoder/chat` handler 中 `event.wait(90)` 会阻塞当前线程，如果使用单线程 HTTPServer 则 `/qoder/respond` 请求无法到达，造成死锁。

API 端点：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/qoder/pending` | GET | 获取待处理请求 |
| `/qoder/chat` | POST | 提交聊天请求（阻塞等待回复，最多 90 秒） |
| `/qoder/respond` | POST | 提交回复（由桥接脚本调用） |

---

### 3. 智能搜索系统

当远程模型无法回答实时性问题时，系统自动触发搜索重试。搜索系统有三层：触发判定 → 搜索引擎 → 重试注入。

#### 3.1 搜索触发 — 三条路径

**路径一：模型主动搜索标记**

模型在回复中输出 `[SEARCH: 搜索关键词]` 标记，系统提取关键词执行搜索后回注结果。这条路径让模型自己判断何时需要搜索，精度最高。

**路径二：回避自动检测**

`response_is_evasive(reply)` 检测到模型回复含回避特征（见 3.2 节），自动触发搜索重试，最多 2 轮。

**路径三：关键词 + 短回复**

`needs_web_search(user_msg)` 匹配用户消息中的搜索关键词（天气、新闻、最新、今天等），且模型回复较短（< 50 字）时，触发 1 轮搜索尝试。

auto_reply.py 还保留了 `needs_web_search()` 关键词检测作为搜索意图预判，用于提前准备搜索上下文：

```python
SEARCH_KEYWORDS = [
    "天气", "新闻", "最新", "搜索", "查一下", "帮我查", "搜一下",
    "实时", "今天", "现在", "最近", "刚刚", "昨天", "明天",
    "多少钱", "股价", "汇率", "比分", "热搜", "排行",
    "联网", "网上", "百度", "谷歌", "搜素", "查找",
    "发生了什么", "怎么了", "出事", "突发",
]
```

#### 3.2 回避检测

`response_is_evasive(reply)` 两层检测，带长回复宽容机制：

**短语匹配** — EVASIVE_PHRASES 包含正式表达和口语化变体：

- 正式：无法获取、没有联网、作为AI、无法访问、信息截止...
- 口语：查不到、查不了、网卡了、网不太好、没法帮你、建议你自己...

**正则模式匹配** — EVASIVE_PATTERNS：

```python
r'(?:查|搜|看|找|获取).{0,3}(?:不到|不了|不出来)'  # 查不到/搜不了
r'(?:没有|没).{0,3}(?:联网|网络|实时|最新)'          # 没有联网/没实时
r'(?:我|这边).{0,10}(?:无法|没法|不能|帮不了)'       # 我...没法（放宽间距）
r'(?:网|网络).{0,3}(?:不太好|不好|不行|有问题)'      # 网不太好
r'(?:你自己|自己|你还是).{0,5}(?:看看|查查|搜搜)'    # 你自己看看
```

**长回复宽容机制**：当回复超过 100 字时，仅命中 1 个回避短语不判定为回避（可能是模型在引用搜索结果时提及"无法获取"等词）。需要 2 个以上短语命中才判定。正则模式匹配在长回复中也会被忽略。这大幅减少了误判，避免已经基于搜索结果生成的优质回复被丢弃。

已移除的误判源："实时信息"、"最新数据"（模型引用搜索结果时常用）、"抱歉，我"缩窄为"抱歉，我无法"/"抱歉，我不能"（避免命中"抱歉，我搜了一下"）。

#### 3.3 Tool call 检测

模型可能输出 JSON 工具调用（如 `call\n{"name": "get_weather", ...}`）而非自然语言。三层防护：

1. 正则检测 `^(call|function|tool)\s*\n?\s*\{` 和 `^\s*\{.*"name":.*"arguments"` → 清空 reply
2. tool_call_detected 被视为 evasive，触发搜索重试
3. 最终回复兜底拦截：如果 result 仍含 tool call 格式，替换为自然语言兜底消息

#### 3.4 搜索查询词提取

`extract_search_query()` 从用户消息中去掉指令性词语：

```python
SEARCH_COMMAND_WORDS = [
    "联网查询", "联网搜索", "帮我查一下", "帮我搜一下",
    "帮我查", "帮我搜", "帮我分析", "帮我看看",
    "查一下", "搜一下", "分析一下", "分析下",
    "搜索一下", "查询一下", "一下",
    "联网", "搜索", "查询", "查找", "搜素", "分析",
    "请问", "请帮我", "帮我看", "帮我看看",
    "告诉我", "介绍", "了解", "说说",
]
```

"联网查询分析下本周股市行情" → "本周股市行情"

**日期解析**：搜索词中"今天/明天/昨天/后天"自动替换为具体日期（如 2026年06月21日），让搜索结果更精准。

#### 3.5 天气查询

`is_weather_query()` 检测天气关键词后，天气查询有专用路径：

**auto_reply.py 实现**：优先通过 Bocha 搜索天气信息（国内稳定），回退到 wttr.in API：

```python
# 1. Bocha 搜索天气
bocha_results = _search_bocha(f"{location} 今天天气预报 温度 降水")
# 2. 失败则通用搜索
web_search(f"{location} 今天天气")
# 3. 再失败则 wttr.in API
GET https://wttr.in/{location}?format=j1&lang=zh
```

**shared/search_engine.py 实现**：优先 `weather_web_search()`（走完整搜索瀑布），回退 `weather_search()` (wttr.in)。

`extract_location()` 从查询中提取地名，去掉天气词（天气、气温、温度、下雨...）、日期词（今天、明天...）和指令词（分析、帮我、下...）。

wttr.in 返回数据包含：当前状况（温度、体感温度、天气状况、湿度、风速、能见度）、今日预报（最高/最低温度、逐时段预报）、明日预报。超时设为 20 秒（国内网络常较慢）。

#### 3.6 通用搜索 — 四级瀑布

`web_search()` 按优先级依次尝试四个搜索后端，任一成功即返回：

**第一级：博查 API (Bocha)** — 国内最稳定，需要 API key

```python
POST https://api.bochaai.com/v1/web-search
Headers: Authorization: Bearer {bocha_api_key}
{
  "query": "...",
  "freshness": "noLimit",
  "summary": true,
  "count": 5
}
```

返回 `data.webPages.value` 数组，每条含 `name`（标题）、`summary`/`snippet`（摘要）、`url`（来源）。bocha_api_key 配置在 `model_config.json` 中，未配置时静默跳过并打印调试日志。

**第二级：DuckDuckGo (ddgs 包)** — Bing 后端

```python
from ddgs import DDGS
with DDGS() as ddgs:
    results = list(ddgs.text(query, max_results=5, region="cn-zh", backend="bing"))
```

旧包 `duckduckgo_search` 已更名为 `ddgs`。国内 DuckDuckGo 直连不稳定，使用 Bing 后端。

**第三级：Bing 网页抓取**

```python
GET https://cn.bing.com/search?q={query}&ensearch=0
```

HTML 解析选择器：标题 `<h2><a>`，摘要 `<div class="b_caption"><p>`。

**第四级：百度网页抓取**（仅 shared/search_engine.py）

```python
GET https://www.baidu.com/s?ie=utf-8&wd={query}
```

HTML 解析选择器：标题 `<h3><a>`，摘要 `span.content-right_*` 或 `div.c-abstract*`。

注意：auto_reply.py 的搜索瀑布为三级（Bocha → ddgs → Bing），不含百度。shared/search_engine.py 为四级（含百度兜底）。

#### 3.7 搜索重试流程

```
首次 API 调用
    ↓
检查回复:
    ├── 含 [SEARCH: ...] 标记? → 提取搜索词 → web_search → 注入结果重试
    ├── response_is_evasive() 命中? → 搜索重试循环 (最多 2 轮)
    │       ├── 天气查询 → Bocha 搜索 → web_search → wttr.in
    │       └── 其他查询 → web_search (四级瀑布)
    │       ↓
    │   构造搜索上下文 → 第二次 API 调用
    │       ↓
    │   新回复不回避? → 使用新回复
    │   仍回避但更长? → 记为 best_reply 候选
    │   仍有下一轮? → 用原始消息重试
    └── needs_web_search() + 回复<50字? → 搜索重试 (1 轮)
    ↓
best_reply 兜底: 如果所有重试仍回避，用过程中最长的非空回复
最终 tool call 拦截: 仍含 JSON 工具调用格式 → 替换为自然语言兜底
```

搜索重试 prompt 关键设计：

```
你是一个智能助手。以下是系统通过实时网络搜索获取到的最新信息。

当前时间: 2026年06月21日 14:30

===== 搜索结果 =====
[搜索结果文本]
===== 搜索结果结束 =====

重要要求：
1. 你必须基于以上搜索结果来回答用户的问题。
2. 绝对不要说你无法搜索、没有联网、查不到或获取不到——数据已经在上面了。
3. 如果搜索结果中包含天气、新闻等实时数据，直接告诉用户具体内容。
4. 用自然口语化的方式回复，简洁明了。
```

搜索链路全程日志以 `[搜索链路]`、`[搜索判定]`、`[搜索]` 前缀标记。

---

### 4. Qoder 中继桥接

#### qoder_bridge.py

独立运行的桥接脚本，每 2 秒轮询中继服务器的 `/qoder/pending` 端点。检测到待处理请求后，调用 Ollama 生成回复并提交到 `/qoder/respond`。

```bash
python qoder_bridge.py --port 11435 --model qwen3:8b
```

#### qoder_long_poll.py

供 QoderWork cron 任务调用的长轮询脚本。每 3 秒检查一次中继服务器，检测到消息后立即输出 JSON 到 stdout 并退出。

```bash
python qoder_long_poll.py --port 11435 --timeout 50
```

QoderWork cron 任务配置：每 5 分钟执行一次，每次做 4 轮 × 50 秒短轮询，避免单次超时被终止（cron 有执行时限，单进程超 ~60 秒会被终止）。

---

### 5. auto_reply.py 核心实现

#### 技术参数

| 项目 | 值 |
|------|------|
| 运行环境 | Python 3.12.10 |
| 依赖库 | requests, wxautoz, ddgs, json, re, time, os, signal, argparse |
| 轮询间隔 | 3 秒 (POLL_INTERVAL) |
| 对话历史 | 每个会话保留最近 10 轮 (MAX_HISTORY) |
| HTTP 服务器 | ThreadingHTTPServer（端口 11435） |
| LLM 参数 | temperature=0.8, max_tokens=4096（远程）/ num_predict=512（Ollama） |

#### 核心流程

```
启动 → argparse 解析参数 → 加载配置 → 模型选择（菜单/CLI 参数/上次配置）
    ↓
初始化微信 (wxautoz) → 获取账号名
    ↓
启动 Qoder 中继 (ThreadingHTTPServer, 后台线程)
    ↓
主循环 (每 3 秒):
    热加载配置 → GetNextNewMessage() → 遍历消息 → 判断类型 → 分发处理
        ↓
    ┌─── 群聊? ──── 是 → 检查 @提及 → 检查群黑白名单 → 生成回复 → 发送
    └─── 私聊? ──── 是 → 检查黑白名单 → 生成回复 → 发送
                                                ↓
                                    ┌─── Ollama 模式 → generate_reply_ollama()
                                    ├─── 远程模式 → generate_reply_remote()
                                    │       └── 智能搜索触发 + 四级搜索瀑布 + 天气 API
                                    └─── Qoder 模式 → 中继等待回复 (90s)
```

#### 独立搜索实现

auto_reply.py 拥有完整的独立搜索代码（约 400 行），包括：EVASIVE_PHRASES、EVASIVE_PATTERNS、response_is_evasive、needs_web_search、extract_search_query、extract_location、is_weather_query、weather_search、_search_bocha、_search_ddgs、_search_bing、web_search、generate_reply_remote 中的搜索重试逻辑。

这些函数与 `shared/search_engine.py` 功能相同但完全独立（不 import），修改搜索逻辑时必须同时更新两处。

差异点：auto_reply.py 的搜索瀑布为 3 级（无百度），shared 为 4 级；auto_reply.py 的 _search_bocha 从 `selected_model` 全局变量读取 bocha_api_key，shared 版本通过函数参数传入。

#### 全局变量作用域

`main()` 函数内修改 `selected_model` 时必须声明 `global`：

```python
def main():
    global wx, self_name, selected_model  # 必须包含 selected_model
```

否则 `selected_model = {...}` 会创建局部变量，`generate_reply()` 读取的仍是模块级默认值。

#### 配置保存 — 合并模式

`save_model_config()` 采用合并模式写入，避免丢失额外配置字段：

```python
def save_model_config(cfg):
    """保存模型选择配置，合并已有字段避免丢失额外配置（如 bocha_api_key）"""
    existing = {}
    if os.path.exists(MODEL_CONFIG_PATH):
        try:
            with open(MODEL_CONFIG_PATH, "r", encoding="utf-8") as f:
                existing = json.load(f)
        except Exception:
            pass
    existing.update(cfg)  # 新值覆盖旧值，未涉及的字段保留
    with open(MODEL_CONFIG_PATH, "w", encoding="utf-8") as f:
        json.dump(existing, f, ensure_ascii=False, indent=2)
```

这在 model_config.json 包含 bocha_api_key、wecom 等非标准字段时至关重要。旧实现直接覆盖整个文件，每次启动都会丢失这些字段。

`--remote` 启动路径也做了字段继承：从 saved_cfg 构造 selected_model 后，遍历 saved_cfg 中的额外字段并复制到 selected_model。

#### 配置热加载

每个轮询周期开始时重新读取 `auto_reply_config.json`，修改后无需重启。

---

### 6. 启动脚本技术细节

所有 .bat 脚本必须使用 GBK 编码、CRLF 换行、无 BOM。原因：cmd.exe 使用系统默认代码页 (中文 Windows = 936/GBK) 解析批处理文件，UTF-8 多字节中文会导致命令截断。

用 Python 写入 bat 文件：

```python
open(path, 'wb').write(content.encode('gbk'))
```

#### 统一 PATH

```batch
set PATH=D:\software\nodejs;%LOCALAPPDATA%\Programs\Python\Python312;%LOCALAPPDATA%\Programs\Python\Python312\Scripts;%LOCALAPPDATA%\Programs\Ollama;%APPDATA%\npm;%PATH%
```

#### start.bat

支持远程/本地模型选择菜单。远程模式从 `workspace/model_config.json` 读取 `api_base` 和 `api_key`，再设置 `OPENAI_API_BASE_URL` 和 `OPENAI_API_KEY` 环境变量供 Open WebUI 使用，跳过 Ollama 启动；启动脚本本身不保存密钥。

端口等待使用 `goto` 标签循环 + `curl` 检测，不用 `for /L` 嵌套 `if+errorlevel`（cmd.exe 会报语法错误）。

---

### 7. 网络端口一览

| 端口 | 服务 | 协议 | 访问地址 |
|------|------|------|-----------|
| 11434 | Ollama | HTTP REST | http://127.0.0.1:11434/api/chat, /api/tags |
| 11435 | Qoder 中继 | HTTP REST | http://127.0.0.1:11435/qoder/* |
| 8000 | MCPo | HTTP REST | http://127.0.0.1:8000/weixin/{tool_name} (可选) |
| 3000 | Open WebUI | HTTP | http://127.0.0.1:3000 (可选) |
| 8080 | WeCom 回调 | HTTP | http://0.0.0.0:8080/callback (企业微信自建应用) |
| 8081 | 微信客服 | HTTP | http://0.0.0.0:8081/callback (微信客服回调) |

本地服务（Ollama、Qoder 中继、MCPo、Open WebUI）仅监听 127.0.0.1，不对外暴露。WeCom 回调服务监听 0.0.0.0 以接收企业微信云端推送。

---

### 8. 环境依赖汇总

| 软件 | 版本 | 安装方式 | 用途 |
|------|------|----------|------|
| Python | 3.12.10 | 官方安装器 | 运行所有 Python 脚本 |
| wxautoz | — | pip install wxautoz | 个人微信 UI 自动化 |
| ddgs | — | pip install ddgs | DuckDuckGo 搜索（搜索瀑布第二级） |
| requests | — | pip install requests | HTTP 调用 |
| Ollama | v0.30.6 | 官方安装器 | 本地 LLM 推理 |
| Node.js | v22.14.0 | 手动安装至 D:\software\nodejs | 运行 MCPo/weixin-mcp（可选） |
| Open WebUI | v0.9.6 | uv pip install open-webui | Web 聊天界面（可选） |
| MCPo | v0.0.20 | uv pip install mcpo | MCP→OpenAPI 代理（可选） |
| qwen3:8b | — | ollama pull qwen3:8b | 通义千问 8B 本地模型 |
| curl | — | Windows 内置 | 端口就绪检测 |
| wechatpy | — | pip install wechatpy | 企业微信消息加解密 (wecom/) |
| Flask | — | pip install flask | WeCom 回调 HTTP 服务器 |
| wecom-aibot-sdk | — | pip install wecom-aibot-sdk | 企业微信智能机器人 WebSocket (wecom_bot/) |
| cryptography | — | pip install cryptography | wechatpy 加解密依赖 |

---

### 9. 配置管理

#### model_config.json（workspace/，不进 git）

```json
{
  "mode": "remote",
  "model": "qwen3.7-max",
  "api_base": "https://z.apiyihe.org/v1",
  "api_key": "sk-xxx",
  "remote_provider": "Gemini Flash (apiyihe)",
  "bocha_api_key": "sk-xxx"
}
```

| 字段 | 说明 |
|------|------|
| `mode` | `ollama`（本地）/ `remote`（远程 API）/ `qodercli`（Qoder CLI）/ `qoder`（Qoder 中继） |
| `model` | 模型名称 |
| `api_base` / `api_key` | 远程 API 连接信息 |
| `bocha_api_key` | 博查搜索 API key（可选，无 key 时搜索瀑布跳过 Bocha） |

远程部署时 git pull 不会更新此文件（gitignored），须手动同步。`_search_bocha` 无 key 时静默跳过并打印调试日志。

#### wecom_config.json / wecom_bot_config.json（workspace/，不进 git）

分别存放企业微信自建应用和智能机器人的认证凭据，由 `shared/config.py` 加载。

#### customer_service_config.json（workspace/，不进 git）

微信客服模块配置，包含认证凭据和转发设置：

```json
{
  "corp_id": "ww202e2b0f068207c8",
  "corp_secret": "8hnirjaVzUDe...",
  "token": "HCVRIfDtR4kDsPq83Bu",
  "encoding_aes_key": "BfJeIkRFtAHGk...",
  "host": "0.0.0.0",
  "port": 8081,
  "open_kfid": "wkMwoqZgAAUFPNUXBgN5HcDK_9_gHLjw",
  "forward_chatid": "",
  "forward_userid": "ZhouLei",
  "agent_id": "1000002"
}
```

| 字段 | 说明 |
|------|------|
| `open_kfid` | 客服账号 ID（不配置时自动从 kf/account/list 获取第一个） |
| `forward_chatid` | 转发到内部群的 chatid（需先通过 appchat/create 创建） |
| `forward_userid` | 转发到个人用户的 userid（优先级高于 forward_chatid） |
| `agent_id` | 自建应用的 agentid（forward_userid 必填） |

---

### 10. 扩展指南

#### 添加新的远程模型

在 `auto_reply.py` 中的 `REMOTE_MODELS` 列表添加模型名称即可（前提是该模型在 apiyihe 代理中可用）：

```python
REMOTE_MODELS = [
    "gemini-3.5-flash",
    "glm-5.1",
    "qwen3.7-max",
    "new-model-name",  # 新增
]
```

#### 添加新的搜索后端

在 `web_search()` 函数中插入新的搜索函数调用。注意搜索瀑布的优先级：国内环境推荐 Bocha 优先（API 稳定、返回结构化数据），DuckDuckGo/Bing 作为后备。

由于 auto_reply.py 和 shared/search_engine.py 各自独立实现搜索，新增后端必须同时修改两处。

#### 添加新的搜索意图关键词

在 `SEARCH_KEYWORDS` 列表中添加。

#### 添加新的回避检测短语/模式

在 `EVASIVE_PHRASES` 或 `EVASIVE_PATTERNS` 中添加。注意：

- 避免过于宽泛的短语（如"实时信息"、"最新数据"），会导致搜索结果引用被误判
- "抱歉，我"类短语应缩窄为具体变体（如"抱歉，我无法"），避免命中"抱歉，我搜了一下"
- 正则模式间距 `.{0,N}` 越大越宽松，建议不超过 10
- 长回复宽容机制（>100 字需 2+ 命中）会自动过滤大部分误判

#### 替换天气 API

修改 `weather_search()` 函数。当前优先走 Bocha 搜索获取天气数据（国内更稳定），wttr.in 作为补充数据源。替换为其他天气 API（如和风天气、OpenWeatherMap）时保持返回格式为纯文本即可。

#### 持久化对话历史

当前对话历史仅存内存中的 `defaultdict(list)`，重启丢失。可扩展为写入 SQLite 或 JSON 文件。

#### 添加新的微信接入方式

新接入方式只需 import `shared/ai_engine.generate_reply()` 和 `shared/config` 模块，实现消息收发逻辑即可。AI 引擎和搜索能力完全通过 shared/ 模块复用。
